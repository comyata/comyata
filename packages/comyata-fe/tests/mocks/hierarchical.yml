name: Root
nameSelf: ${$self().name}
nameRootA: ${$root().name}

nested:
    nameRootB: ${$root().name}

    count: 111
    countSelf: ${$self().count}
    countRootA: ${$root().nested.count}

    deeper:
        nameRootC: ${$root().name}
        countRootB: ${$root().nested.count}
